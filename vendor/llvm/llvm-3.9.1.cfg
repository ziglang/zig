# Test using a partial commit SHA1
LLVM_CHECKOUT_REF := 41dbbb7c22da5

# NOTE: using recursively expanding assignment `=` not the
# simply expanding assignment `:=` so $(LLVM_BUILD_TYPE) is
# included in LLVM_TGT_TAG.
LLVM_TGT_TAG = v3.9.1-$(LLVM_BUILD_TYPE)

LLVM_PROJECT_LIST := clang;lld
